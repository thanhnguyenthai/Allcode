#args = parameter that will pack all arguments into a tuple
#       usefull so that a function can accept a varying amount of arguments.
# là 1 tham số sẽ đogns gói tất cả các đối số thành 1 bộ
# nó hữu ích để 1 chức năng có thể chấp nhận 1 số lượng khác nhau của các đối số.


#def add(*stuff): # dấu * làm chức năng đóng dấu
#    sum = 0
#    stuff = list(stuff)
#    stuff[0] = 0
#    for i in stuff:
#        sum +=i
#    return sum

#print(add(1,2,3,4,5,6,7,8))